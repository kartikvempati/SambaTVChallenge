# SambaTV Login Page app

Features blocking authentication using local storage as a user db

Uses a Node/Express Server

Install the necessary files with npm install.
Start the node server with nodemon server/server.js

The application will run on localhost: 8000.


